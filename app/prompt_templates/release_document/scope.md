{{> _shared_instructions.md }}

Write section: **Scope**.

Include:
- What activities/lifecycle stages are covered (plan/build/verify/validate/release/maintain).
- Boundaries: where/when the tool is used, exclusions, intended environment/users if known.

If unknown, write "N/A" and what info is needed.