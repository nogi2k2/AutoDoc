{{> _shared_instructions.md }}

Write section: **Trace Matrix**.

Construct a requirements trace matrix based on the CONTEXT. Output a Markdown table with exactly these headers:

| Requirement ID | Requirement Reference | Test Protocol | Test Protocol Version | Test Record Reference | Test Record Reference Version |

Populate the table linking requirements to their respective test protocols and records. 
If specific version numbers or references are missing, insert "TBD". If no traceability data is available, output the empty table format and state "TBD".